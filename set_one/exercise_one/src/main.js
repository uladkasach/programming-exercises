/**
	@file This is the main file for this exercise, utilizing all created resources to simulate the users behavior and output the state into the consoles.

	@description
	This file is to be imported into an environment provisioned with the following utilities:
		- utils/visual_console

	The provisioning is to be done either through JSFiddle directly or in the `index.html` file.
**/

var output_console = new Visual_Console(window.document.querySelector('#output'));
console.log("here i am");
output_console.log("test log")
